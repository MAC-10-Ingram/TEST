<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.happyhouse.mapper.UserMapper">

	<insert id="addUser" parameterType="UserInfoDto" >
		insert into userinfo(id, password, email, name, lat, lng)
		values(#{id}, #{password}, #{email}, #{name}, #{lat}, #{lng})
	</insert>
	
	<select id="selectUser" parameterType="string" resultType="UserInfoDto">
		select id, name, email, password
		from userinfo
		where id = #{id}
	</select>
	
	<update id="updateUser" parameterType="UserInfoDto">
		update userinfo set email = #{email}, password = #{password}, name = #{name}
		where id = #{id}
	</update>
	
	<delete id="deleteUser" parameterType="string">
		delete from userinfo
		where id = #{id}
	</delete>
	
	<select id="login" parameterType="UserInfoDto" resultType="UserInfoDto">
		select id, name, email
		from userinfo
		where id = #{id} and password = #{password}
	</select>

</mapper>